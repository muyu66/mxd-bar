❯ 使用rust为我开发mxd-bar软件。
主页面是带有圆角、可悬浮、置顶、无边框的长方形小卡片，宽度自适应，高度72px
布局是从左到右分3个区，
第一区上方是 实时EXP/分 文字，下方是数字，比如 33,622
第二区上方是 预估EXP/时 文字，下方是数字，比如 33,622
第三区是按钮区，里面是两行多列的小按钮，分别是 上报数据、管理数据、999打卡、神秘商人、BOSS、X（点击X后会关闭程序）

上报数据按钮：
点击上报数据按钮之后，整个主页面会进入全新的上报数据页面，布局从左到右依次是：（上方是标签文字，下方是各种输入框）
等级（正整数输入框）、职业（选择框，数据来源 data\jobs.json）
地图（输入和选择框二合一，data\maps.json，具有搜索功能，展示的是mapName，传递数据也是使用mapName，只读取 scene=hunting，用户也可以自行输入）
模式（组队、单人开关选项）
攻击力/魔法力（正整数输入框，如果职业选择的是魔法师系，则改为魔法力，其他情况默认是攻击力）
备注（文本框，限制20个字符）
分割线
确认提交（按钮，点击后整理json数据（包含 预估EXP/时，当进入这个页面时，预估EXP/时 就应该立即被缓存下来，等待确认提交时一并提交。另外还有 预估EXP/时 对应的至多次数也要一并传输，并转换为测试时长属性，算法是 至多次数 \* 10，也就是多少秒的意思），上报远程服务器接口，暂不实现。跳转至上报数据成功页面）
取消（按钮，点击后返回之前的主页面）
注意：等级、地图、模式、攻击力/魔法力这些数据，在用户点击确认提交按钮之后，需要保存数据到 %APPDATA%\mxd-bar\data.ini，下次再打开上报数据页面时，自动读取这里的数据。

上报数据成功页面：
上方显示 绿色的网址（来自于之前上报远程服务器接口给予的返回值，暂不实现，占位）
下方显示：返回（按钮，点击后返回主页面）、前往查看（按钮，暂不实现，功能是和点击上方的绿色网址一样，调用浏览器打开）、管理数据（按钮，暂不实现，功能也是调用浏览器打开一个网址）

管理数据按钮：
将用户主板序列号hash32位小写，作为用户的唯一ID。然后打开一个网址附加在URL上（暂不实现）

999打卡按钮：
根据剩余时间显示文字，比如 999打卡 3m （999打卡 2h、999打卡 34s）
算法是，一开始没有数据时，显示 “999打卡 无”，用户点击后，弹出一个时间选择框，可以输入时分（默认为当前时分）。将这个时分加上当前的年月日存储在 %APPDATA%\mxd-bar\data.ini 。此时之前说的999打卡按钮显示的就是此时此刻与这个存储日期的差距，只显示2位数加单位，比如33s、55m、12h，实时显示。
如果此时此刻和记录的时间差距达到负数，也就是超过了1天，则按钮变为 999 待打卡，按钮字体颜色和边框颜色变成红色且抖动直到用户重新设置时间。

神秘商人按钮：
UI和999打卡一样，不过算法不一样。算法是，用户点击弹框输入一个时分（默认为05:59），然后基于这个时分进行倒数，当为0或者小于0时，显示 绿色的 “神秘商人 已刷新”。其他都是显示 “神秘商人 23m”之类。
数据需要存储在 %APPDATA%\mxd-bar\data.ini

BOSS按钮：
UI和999打卡一样，不过算法不一样。算法是，用户点击弹框输入一个时分（默认为当前时分），，然后基于这个时分进行计时，“BOSS 13s”，“BOSS 22m”，当计时超过99m时，说明该BOSS已经废弃，显示为“BOSS 无”。
数据需要存储在 %APPDATA%\mxd-bar\data.ini

主题：黑色主题、按钮主颜色为橘黄色。

核心功能EXP计算（不需要存储数据）：
之前的第一区、第二区需要显示经验的计算。具体方式是对 Maplestory_Classic.exe 进程进行截图（注意这个游戏反外挂特别严格，一定不能读色块或者读内存），然后分析出 EXP 所对应的数字。
我在根目录提供了 test1.png、test2.png、test3.png。test1.png是游戏窗口的真实截图，test2.png是我们需要分析的区域。不要用定点截取出EXP数字，因为不同的分辨率，位置可能不一样。test3.png是等级
获取EXP数字是第一步，第二步是获取等级数字，以此为基础，10秒获取一次EXP、等级数据。
实时EXP/分 的算法是：展示最近至多6次的10秒EXP之和，如果最近次数不到6次，则按照除以次数后乘以6来模拟。（比如我只打了20秒，分别是130EXP、20EXP，则实时EXP/分的计算是 (130+20)/2\*6 ）
预估EXP/时 的算法是：跟 实时EXP/分 算法一样，区别是会采用至多360次最新记录来计算，如果最近次数不到360次，则按照除以次数后乘以360来模拟。
